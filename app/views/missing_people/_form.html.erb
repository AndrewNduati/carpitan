<%= form_with(model: missing_person) do |form| %>
  <% if missing_person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(missing_person.errors.count, "error") %> prohibited this missing_person from being saved:</h2>

      <ul>
      <% missing_person.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <% if missing_person.persisted? %>
      <div class="float-end">
        <%= link_to 'Destroy', missing_person, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if missing_person.persisted? %>
      <%= link_to "Cancel", missing_person, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", missing_people_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
